{
  "name": "@klon/example-mini-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt --check",
    "format:fix": "oxfmt",
    "typecheck": "tsgo --noEmit -p .",
    "check": "pnpm run lint && pnpm run format && pnpm run typecheck",
    "fix": "pnpm run lint:fix && pnpm run format:fix"
  },
  "dependencies": {
    "@pocketsign/in-app-sdk": "^2.0.0-beta.1",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "vite": "catalog:"
  }
}
